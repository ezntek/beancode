// NOTE: this is a recursive implementation of the factorial.
// Recursion is not in the IGCSE syllabus.

FUNCTION factorial(num: INTEGER) RETURNS INTEGER
    IF num <= 1
      THEN
        RETURN 1
      ELSE
        RETURN num * factorial(num - 1)
    ENDIF
ENDFUNCTION

OUTPUT factorial(5)
